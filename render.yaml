services:
  - type: web
    name: campus-trade-api
    env: node
    rootDir: server
    buildCommand: npm install
    startCommand: node src/index.js
    envVars:
      - key: DB_DIALECT
        value: sqlite
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
